module.exports = {
  // user: async (parent, args, { prisma, req, res }) => {
  //   return await prisma.user(args);
  // },
  // users: async (parent, args, { prisma, req, res }) => {
  //   return await prisma.users();
  // }
};
